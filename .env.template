# TickTick OAuth credentials (from developer.ticktick.com)
TICKTICK_ACCESS_TOKEN=
TICKTICK_CLIENT_ID=
TICKTICK_CLIENT_SECRET=

# Google Calendar OAuth credentials file (local path to gcp-oauth.keys.json)
GOOGLE_OAUTH_CREDENTIALS_FILE=
# Optional: comma-separated tool allowlist (e.g. list-events,get-today-events,create-event)
GOOGLE_CALENDAR_ENABLED_TOOLS=

# OpenClaw Anthropic API key
ANTHROPIC_API_KEY=

# Telegram Bot Token
TELEGRAM_BOT_TOKEN=

# Hetzner SSH alias
HETZNER_SSH_HOST=hetzner-main

# CLI access — openclaw-chat local client
# Gateway token: retrieve with `ssh hetzner-main "openclaw config get gateway.auth.token"`
OPENCLAW_GATEWAY_TOKEN=
# SSH host to tunnel through (default: value of HETZNER_SSH_HOST)
# The tunnel is started automatically when the gateway port is not reachable
OPENCLAW_SSH_HOST=hetzner-main
# Port the openclaw gateway listens on (default: 18789)
OPENCLAW_GATEWAY_PORT=18789
# Session key — must match the key the Telegram bot uses for your account
# Check with: ssh hetzner-main "openclaw sessions list" (look for the most recent non-cron session)
OPENCLAW_SESSION_KEY=main
